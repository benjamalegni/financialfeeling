/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You can add other Next.js configurations here if needed
  // For example, experimental features or webpack customizations.
};

export default nextConfig;
